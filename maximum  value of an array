#include<stdio.h>
int main()
{
    int arr[5]={1,4,6,7,9},i,max;
    max=arr[0];
    for(i=1;i<5;i++)
    {
         max= (max>arr[i]) ? max: arr[i];
    }
    
printf("the maximum score is %d",max);
}
