#include<stdio.h>
#include<string.h>
int main()
{
    char string[9]="bhargavi",temp,i,j;
    for(i=0,j=8;i<=4,j>4;i++,j--)
    {
        temp=string[i];
        string[i]=string[j];
        string[j]=temp;
    }
    for(i=0;i<9;i++)
    {
        printf("%c",string[i]);
    }
}
    
