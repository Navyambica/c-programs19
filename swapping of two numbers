#include <stdio.h>

int main() {
    int a=1,b=2,temp;
    printf("a=%d\n",a);
    printf("b=%d\n",b);
    temp=a;
    a=b;
    b=temp;
    printf("the swapping of a number is a=%d\n",a);
    printf("the swapping of b number is b=%d\n",b);
return 0;
}
    
