#include<stdio.h>
int main()
{
    int arr[5]={1,4,6,7,9},i,min;
    min=arr[0];
    for(i=1;i<5;i++)
    {
         min= (min<arr[i]) ? min: arr[i];
    }
    
printf("the minimum score is %d",min);
}
